# ─────────────────────────────────────────────────────────────
# App
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME="Room Rental System"
NEXT_PUBLIC_APP_VERSION="1.0.0"
NODE_ENV=development
# (ทางเลือก) ใช้กับลิงก์ canonical/redirect ต่าง ๆ
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ─────────────────────────────────────────────────────────────
# Firebase (Client SDK) — ใช้ในฝั่ง Browser/Client Components
# ค่านี้ดึงจาก Firebase Console → Project settings → General → Your apps (Web)
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_APP_ID=1:xxxxxxxxxxxx:web:yyyyyyyyyyyyyy
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=xxxxxxxxxxxx

# ─────────────────────────────────────────────────────────────
# Firebase Admin SDK (Server) — ใช้ใน Server Components/Route Handlers
# ค่านี้มาจาก Service Account (Project settings → Service accounts → Generate new private key)
# *** สำคัญ: ใส่ PRIVATE KEY แบบมี \n แทนบรรทัดใหม่ตามตัวอย่างด้านล่าง ***
# ─────────────────────────────────────────────────────────────
FIREBASE_ADMIN_PROJECT_ID=your-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvgIBADANBgkqhkiG9w0BAQEFAASCBK...\n-----END PRIVATE KEY-----\n"

# (ทางเลือก) ชื่อคุกกี้ session เมื่อทำ Firebase Session Cookie
# AUTH_SESSION_COOKIE_NAME=app_session

# ─────────────────────────────────────────────────────────────
# Sentry (Client + Server)
# วิธีได้ค่า DSN: Sentry.io → Project Settings → Client Keys (DSN)
# ถ้าไม่ตั้ง DSN ระบบจะไม่ส่งข้อมูล (ถือว่า disabled)
# ─────────────────────────────────────────────────────────────
SENTRY_DSN= # server ใช้
NEXT_PUBLIC_SENTRY_DSN= # client ใช้ (อนุญาตเผยแพร่ได้)
SENTRY_ENVIRONMENT=development
# ปล่อยว่างเพื่อใช้ค่า version จาก process.env.NEXT_PUBLIC_APP_VERSION หรือ Git SHA ก็ได้
SENTRY_RELEASE=

# Sampling (ปรับให้เหมาะกับ prod)
SENTRY_TRACES_SAMPLE_RATE=0.1       # APM / Distributed Tracing
SENTRY_PROFILES_SAMPLE_RATE=0.1     # Profiling
SENTRY_DEBUG=false

# อันนี้ใช้ตอน build เพื่ออัปโหลด source maps (อย่า commit ของจริง)
# ต้องมีถ้าคุณตั้งให้ next/sentry อัปโหลดอัตโนมัติ
SENTRY_AUTH_TOKEN=

# ─────────────────────────────────────────────────────────────
# Next.js (optional tweaks)
# ─────────────────────────────────────────────────────────────
# NEXT_TELEMETRY_DISABLED=1
