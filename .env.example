# ─────────────────────────────────────────────────────────────
# App
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME=Room Rental System
NEXT_PUBLIC_APP_VERSION=1.0.0
NODE_ENV=development
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ─────────────────────────────────────────────────────────────
# Firebase (Client SDK) — ใช้บนฝั่ง Browser
# **ค่าชุดนี้ให้เอามาจาก Firebase Console → Project settings → Your apps (Web)**
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...yourKeyHere...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_APP_ID=1:1234567890:web:abcdef123456
# เริ่ม Oct 30, 2024 เป็นต้นมา บางโปรเจ็กต์จะได้ bucket แบบ firebasestorage.app (ทั้งสองรูปแบบใช้ได้)
# อ้างอิง: docs ชี้ว่าบัคเก็ตใหม่ใช้ your-project.firebasestorage.app
# https://firebase.google.com/docs/storage/web/start
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=1234567890
# ถ้าไม่ได้เปิด Analytics ใส่หรือไม่ใส่ก็ได้
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXX

# ─────────────────────────────────────────────────────────────
# Firebase Admin SDK (Server) — ใช้เฉพาะฝั่งเซิร์ฟเวอร์
# Service Account: Project settings → Service accounts → Generate new private key
# ใส่เป็นสตริงบรรทัดเดียว ใช้ \n แทนขึ้นบรรทัดใหม่ (โค้ดฝั่งเรา replace(/\\n/g, '\n') ให้แล้ว)
# **ห้ามใส่ NEXT_PUBLIC_** และห้ามใช้ใน client
# ─────────────────────────────────────────────────────────────
FIREBASE_ADMIN_PROJECT_ID=your-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n<your-key-contents>\n-----END PRIVATE KEY-----\n

# (ทางเลือก) ตั้งชื่อคุกกี้ session (ไม่ตั้งก็ใช้ __session)
# SESSION_COOKIE_NAME=__session

# ─────────────────────────────────────────────────────────────
# Sentry (ถ้าใช้)
# ─────────────────────────────────────────────────────────────
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_RELEASE=
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1
SENTRY_DEBUG=false
SENTRY_AUTH_TOKEN=
